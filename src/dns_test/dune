(test
  (name       test)
  (libraries  dns ounit pcap-format bigarray bigarray-compat)
  (deps       (glob_files *.pcap) (glob_files *.zone))
  (preprocess (pps ppx_cstruct)))

(rule
 (alias  runtest)
 (action (run ./test.exe)))
